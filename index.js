"use strict";

const addBudgetButton = document.getElementById("add-budget");
const addHoursButton = document.getElementById("add-hours");

addBudgetButton.addEventListener("click", handleClickAddBank("budget"))
addHoursButton.addEventListener("click", handleClickAddBank("hours"))

const handleClickAddBank = (bankType) => {

};

/**
 * Handle  storing and adding new bank to user dash from user input
 * @param {string} bankType Which bank category to add to
 */
const addBank = (bankType) => {
  //
};
